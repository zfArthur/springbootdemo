<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
            http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 用dubbo协议在20881端口暴露服务 -->
    <dubbo:protocol name="dubbo" port="2081" charset="UTF-8" payload="16777216" threads="400"/>

    <!-- 声明需要暴露的服务接口 -->

    <!--    <dubbo:service protocol="dubbo" interface="com.meicai.dmall.ladder.api.service.DemoApi"-->
    <!--                   class="com.meicai.dmall.ladder.domain.appoint.apiImpl.DemoApiImpl"-->
    <!--                   version="${dubbo.provider.version}" loadbalance="random" retries="0"/>-->

    <dubbo:service interface="com.example.demo.dubbo.HelloService"
                   ref="HelloService"
                   version="${dubbo.provider.version}"
                   protocol="dubbo"
                   group="appoint"/>
</beans>